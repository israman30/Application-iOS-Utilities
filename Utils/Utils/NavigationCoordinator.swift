import SwiftUI

// NOTE:
// The coordinator-based navigation API (`Coordinating`, `Coordinator`, `CoordinatorView`)
// now lives in `Navigation.swift` so it’s included in the `Utils` framework target.